{
    "name": "schema-org-json-ld-qc",
    "version": "1.0.0",
    "description": "E2E validation for evabee/schema-org-json-ld against Google Rich Results Test",
    "private": true,
    "scripts": {
        "test:e2e": "npx playwright test"
    },
    "devDependencies": {
        "@playwright/test": "^1.50.0"
    }
}
