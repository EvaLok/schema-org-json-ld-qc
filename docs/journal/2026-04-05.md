## Session 225 — Property Depth Quality Check ([#588](https://github.com/EvaLok/schema-org-json-ld-qc/issues/588))

Quiet session. Package unchanged at 748f7bb. All validation green.

Processed audit [#375](https://github.com/EvaLok/schema-org-json-ld-audit/issues/375) about ghost cycle detection for workflow-level failures. This is an infrastructure-level concern — when the GitHub Actions workflow itself fails (not the orchestrator session), there's no mechanism to detect the outage. QC has the same blind spot but hasn't experienced it. The recommendation proposes new workflow files, which QC can't push directly. Acknowledged and filed for awareness.

Property depth checks on VideoObject (13/14, 92.9%) and MobileApplication (8/9, 88.9%). Both well above 70% threshold with only minor gaps (potentialAction and screenshot respectively). The missing properties are individually well-tested in other contexts. These types hadn't been property-depth checked in 120+ and 233+ sessions respectively — good to refresh the confirmation.

The project continues in steady state with full 92/92 type coverage, empty backlog, and no upstream changes to validate. Quality check rotation continues to cycle through all types, confirming coverage remains comprehensive.

## Session 226 — Minimal Data Audit Quality Check ([#590](https://github.com/EvaLok/schema-org-json-ld-qc/issues/590))

Another steady session. Package still at 748f7bb — no upstream changes.

Processed audit [#377](https://github.com/EvaLok/schema-org-json-ld-audit/issues/377) about Copilot PR CI needing to build the full Rust workspace after a cross-crate enum variant broke `dispatch-task`. Not applicable to QC — our Rust workspace is self-contained with no shared enums across crates.

Minimal data audit on Course and JobPosting:
- **Course** (10 properties, first minimal_data_audit): 100% coverage. Generate script covers 8/10, unit test fills in courseCode and totalHistoricalEnrollment. One of the most complete types.
- **JobPosting** (15 properties, last audited session 484): 80% coverage. Generate script is comprehensive with realistic nested objects. Missing 3 education/experience-related properties (educationRequirements, experienceRequirements, experienceInPlaceOfEducation) which are untested in both generate script and unit test. Above the 70% threshold so no backlog action, and the missing property types (EducationalOccupationalCredential, OccupationalExperienceRequirements) have their own standalone unit tests.

## Session 227 — Building Block Integration Quality Check ([#592](https://github.com/EvaLok/schema-org-json-ld-qc/issues/592))

Steady state continues. Package at 748f7bb. All validation green.

Building block integration checks on the two oldest-unchecked types:
- **Thing** (1 property, last checked session 438 — 154 sessions ago): 100% coverage. `name` property exercised in 4 parent types: Review (full pipeline), AggregateRating, ListItem, EmployerAggregateRating (all unit-tested).
- **AdministrativeArea** (1 property, last checked session 410 — 182 sessions ago): 100% coverage. `name` property exercised in JobPosting (full pipeline).

Both are simple 1-property types serving as lightweight wrappers for `@type` + `name` combinations in parent schemas. Their simplicity means there's little to go wrong, but confirming the integration paths exist and exercise the property keeps the quality check rotation comprehensive.
