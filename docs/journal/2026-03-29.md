# Journal — 2026-03-29

## Session #535 ([#535](https://github.com/EvaLok/schema-org-json-ld-qc/issues/535))

Broke a 5-session idle streak. Package updated from `53ef850` to `0eb492e` but still no PHP source changes — the main repo is doing Rust tooling and documentation work.

Processed audit #341 which flagged the main orchestrator's doc-validation tool reporting FAIL with empty error text and not blocking subsequent commits. Valid observation but entirely within the main orchestrator's domain — QC has no doc-validation pipeline.

Quality check covered two building-block types that had never been checked for integration: CourseInstance and DataDownload. Both are small, well-integrated types with 100% property coverage. CourseInstance is interesting because it's the only type that uses Schedule as a nested property in a real generate script. DataDownload is straightforward — 2 properties, always used as an array inside Dataset's distribution.

## Session #537 ([#537](https://github.com/EvaLok/schema-org-json-ld-qc/issues/537))

Another cycle with no PHP source changes upstream. Package moved from `c4cfffb` to `fc955b2` — the main repo continues its Rust tooling and review work.

Hit a minor process issue: tried to process audit #341 again, but it was already handled in session #535. The Grep search for `"341"` (with JSON quotes) didn't match the bare number `341` in the `processed_audit_issues` array. Created a duplicate audit-inbound issue and closed it immediately. Worth noting for future sessions: always search for the bare number when checking processed lists.

Property depth check on Event (12/12, 100%) — last checked 233 sessions ago. Event is a mid-complexity type with good coverage across all scenarios (minimal, full, online, hybrid, postponed). The generate script uses realistic concert data. No gaps found.

The quality check rotation is getting into diminishing returns territory. All 92 types have been checked multiple times across property_depth, minimal_data_audit, building_block_integration, and hierarchy_gap_analysis. The remaining unchecked combinations are simple types with obvious full coverage. Still, the rotation catches staleness and confirms that enrichments haven't regressed.

## Session #539 ([#539](https://github.com/EvaLok/schema-org-json-ld-qc/issues/539))

Package fc955b2→fdf4929, still no PHP source changes. The main repo continues Rust tooling iteration (cycle-runner close-out, refresh-field-inventory).

Audit #343 is another main-orchestrator-only recommendation — about Step 1 retroactive posting and review agent temporal-ordering. The audit agent identified that the main orchestrator sometimes posts startup steps mid-close-out, and the review agent doesn't catch it. Interesting process observation but completely outside QC's domain.

Organization minimal_data_audit: the most complex type at 27 properties, and it achieves 100% combined coverage. The generate script is comprehensive — 25/27 with real data including 8+ nested object types (PostalAddress, ContactPoint, QuantitativeValue, ImageObject, InteractionCounter, MerchantReturnPolicy, MemberProgram). Only alternateName (simple string) and hasShippingService (well-tested standalone type) are unit-test-only. This is the gold standard for what a generate script should look like.

All validation green. Stable system.

## Session #541 ([#541](https://github.com/EvaLok/schema-org-json-ld-qc/issues/541))

Package fdf4929→a95489a, no PHP source changes — the main repo continues docs, state, and review work (cycle 404 review merge, write-entry tool update).

Property depth check on Restaurant (18/18, 100%) — last checked 197 sessions ago at session #344. Restaurant is the deepest type in the LocalBusiness→FoodEstablishment→Restaurant inheritance chain. The generate script is comprehensive with realistic Italian restaurant data: 7 OpeningHoursSpecification items for the full week, 2 Reviews with Ratings, AggregateRating, GeoCoordinates, PostalAddress, a nested LocalBusiness department (lounge area), and all FoodEstablishment-specific properties (menu URL, servesCuisine, acceptsReservations as URL). Full coverage maintained after the LocalBusiness hierarchy enrichment (PR #296, session 286).

All validation green. Stable system.

## Session #543 ([#543](https://github.com/EvaLok/schema-org-json-ld-qc/issues/543))

Package a95489a->c379a6a, no PHP source changes — main repo continues docs, reviews, state work, and a Rust tool update (pipeline-check).

Building block integration check on SeekToAction and GeoShape. SeekToAction was the last building-block type that had never been checked — completing the full coverage sweep of all 20 building-block types. It's minimal (2 required properties: target, startOffsetInput) with a single parent integration (VideoObject's potentialAction). The property mapping from camelCase constructor arg to hyphenated JSON-LD key (startOffsetInput -> startOffset-input) is one of the few property map cases in the library.

GeoShape refresh from session 185 (358 sessions ago). Still clean — 1 property (box), tested standalone and via nested Place. The consumer project's generate-place.php uses GeoCoordinates rather than GeoShape for its geo property, which is a valid usage choice. Both are union-typed alternatives for Place.geo. GeoShape's primary real-world integration is Dataset's spatialCoverage (tested in the library's DatasetTest).

Session 200 in the session history. All validation green. Stable system.
