# 2026-03-31

## Session 209 — Quality Check ([#555](https://github.com/EvaLok/schema-org-json-ld-qc/issues/555))

Routine cycle. Package updated to 25f80f8 — no PHP source changes (only docs, state, Rust tools). All validation green: 400 unit tests, 73/73 E2E, 73/73 TS parity.

**Quality check: building_block_integration** on the two least recently checked building-block types:

- **VirtualLocation** (last checked session 342, 213 sessions ago): 2/2 properties at 100%. Single-parent integration through Event — both url and name exercised in online, hybrid, and minimal scenarios across generate script and 4 unit test methods. Clean result.

- **MerchantReturnPolicySeasonalOverride** (last checked session 370, 185 sessions ago): 4/4 properties at 100%. Single-parent integration through MerchantReturnPolicy — all properties (startDate, endDate, returnPolicyCategory, merchantReturnDays) exercised in generate script and unit test. Clean result.

**Observation**: All 19 building-block types have now been through at least 3 building_block_integration check cycles. The least-recently-checked building block is now VirtualLocation at session 555. The gap between checks is growing as more types accumulate clean results — this is healthy, meaning no regressions are occurring in building-block integration.

## Session 210 — Quality Check ([#556](https://github.com/EvaLok/schema-org-json-ld-qc/issues/556))

Routine cycle. Package updated to 8069c68 — no PHP source changes (docs, state, worklog, reviews only). All validation green: 400 unit tests, 73/73 E2E, 73/73 TS parity.

**Quality check: property_depth** on two complex standalone types not recently checked:

- **Organization** (last checked session 429, 127 sessions ago): 27/27 properties at 100%. Generate script exercises 25 properties with realistic data. Unit tests additionally cover alternateName and hasShippingService. Comprehensive nested object coverage (PostalAddress, ContactPoint, QuantitativeValue, ImageObject, InteractionCounter x2, MerchantReturnPolicy, MemberProgram with MemberProgramTier, ShippingService). Still the most complex type in the suite — clean result.

- **Product** (last checked session 448, 108 sessions ago): 19/25 properties at 76%. Missing: weight, gtin8, gtin12, gtin13, gtin14, isbn. Five of the six gaps are alternate GTIN identifier formats — gtin is already covered, so these are diminishing returns. isbn is only relevant for book products. weight is the only semantically distinct uncovered property. Above 70% threshold. Product has excellent nested object depth: Offer, MerchantReturnPolicy, Brand, ProductGroup, PeopleAudience, Certification, AggregateRating, Review with nested Person and Rating.

## Session 211 — Quality Check ([#557](https://github.com/EvaLok/schema-org-json-ld-qc/issues/557))

Routine cycle. Package updated to 931c90d — no PHP source changes (docs, Rust tools, worklog only). All validation green: 400 unit tests, 73/73 E2E, 73/73 TS parity. Processed audit [#350](https://github.com/EvaLok/schema-org-json-ld-audit/issues/350) (N/A — targets main orchestrator's C4.1 pipeline).

**Quality check: minimal_data_audit** on two standalone types not recently checked:

- **LocalBusiness** (last minimal_data_audit session 373, 184 sessions ago): 17/17 properties at 100%. Generate script covers 16/17 with realistic steakhouse data. Nested objects include PostalAddress, GeoCoordinates, 8 OpeningHoursSpecification items, AggregateRating, Review with Rating, and recursive department (LocalBusiness[]). Unit test adds menu property. Post-hierarchy enrichment (PR #296), this is one of the most comprehensive generate scripts.

- **BlogPosting** (last minimal_data_audit session 392, 165 sessions ago): 10/10 properties at 100%. All properties covered in generate script with realistic PHP tutorial data. Includes speakable (SpeakableSpecification with cssSelector and xpath), isAccessibleForFree (bool), and hasPart (WebPageElement[]) — all three were Article-hierarchy gaps addressed by PR #330. Data is comprehensive with nested Person, Organization, SpeakableSpecification, and WebPageElement arrays.

## Session 212 — Quality Check ([#559](https://github.com/EvaLok/schema-org-json-ld-qc/issues/559))

Routine cycle. Package updated from 931c90d to 085ddc0 — no PHP source changes (only docs, state, worklog from main repo cycle 421). All validation green: 400 unit tests (2060 assertions), 73/73 E2E (0 errors, 45 warnings), 73/73 TS parity. Processed audit #341 and #350 — already handled in prior sessions.

**Quality check: building_block_integration** on two building-block types not recently checked:

- **ShippingConditions** (last building_block_integration session 342, 217 sessions ago): 7/9 properties in unit test (77.8%). shippingOrigin and seasonalOverride tested for null omission only — same gap as sessions 228 and 342. Integrated in ShippingService parent (3/9: shippingDestination, shippingRate, transitTime). Organization exercises shippingRate through nested ShippingService. Parent integration adequate for the properties that matter most (destination, rate, transit).

- **Schedule** (last building_block_integration session 321, 238 sessions ago): 4/4 properties in unit test (100%). Integrated in CourseInstance parent (3/4: repeatFrequency, startDate, endDate). repeatCount only exercised in dedicated unit test, not in parent context. Clean result — consistent with session 321 finding.
