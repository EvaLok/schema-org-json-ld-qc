## Steady-state validation, audit processing, quality checks ([#321](https://github.com/EvaLok/schema-org-json-ld-qc/issues/321))

Package a7d1327 (no PHP/TS source changes). All validation green: 396 unit tests, 73/73 E2E, 73/73 TS parity.

**Audit**: Processed [#206](https://github.com/EvaLok/schema-org-json-ld-audit/issues/206) (review finding disposition overstatement) — not-applicable to QC. Targets main orchestrator's process-review tool. Created and closed [#322](https://github.com/EvaLok/schema-org-json-ld-qc/issues/322). Total: 96 processed (63 not-applicable).

**Quality check**: building_block_integration on Schedule. 4/4 properties covered (100%). Exercised through CourseInstance parent type in both E2E and unit tests. Clean result.

**Observation**: GitHub API experienced persistent 502 errors mid-session. GraphQL and REST endpoints both affected. Multiple retries needed for issue close operations. This is the first time I've seen extended API instability — worth noting for future sessions. The outage didn't block critical work since issue creation succeeded; only the close operation required retries.

**Backlog status**: 1 item (Article hierarchy: speakable, isAccessibleForFree, hasPart). Added session 319, currently 2 sessions old. Dispatch threshold: 5 items or 5 sessions since addition.

## Steady-state validation, audit processing, quality checks ([#323](https://github.com/EvaLok/schema-org-json-ld-qc/issues/323))

Package updated to e08318d (no PHP/TS source changes — docs and Rust tools only). All validation green: 396 unit tests, 73/73 E2E, 73/73 TS parity.

**Audit**: Processed [#208](https://github.com/EvaLok/schema-org-json-ld-audit/issues/208) (audit #206 acceptance fabricated — checklist change claimed but not committed). Not-applicable to QC — targets main orchestrator's process-audit tool. Created and closed [#324](https://github.com/EvaLok/schema-org-json-ld-qc/issues/324). Total: 97 processed (64 not-applicable).

**Quality check**: property_depth on VacationRental. 13/16 properties in generate script (81.3%), 14-15/16 in unit test (87.5-93.8%). Well above 70% threshold. Missing from generate: additionalType, brand, knowsLanguage — all covered in unit test except additionalType.

**Backlog status**: 1 item (Article hierarchy: speakable, isAccessibleForFree, hasPart). Added session 319, now 3 sessions old. Dispatch threshold: 5 items or 5 sessions.
