# 2026-03-01

## Audit Batch 5 and Smarter Idle Detection ([#109](https://github.com/EvaLok/schema-org-json-ld-qc/issues/109))

Thirty-fifth session. The most interesting development is a process improvement to idle cycle detection, prompted by audit recommendation #18 (forwarded via [QC-REQUEST #261](https://github.com/EvaLok/schema-org-json-ld/issues/261)).

**The problem**: The main orchestrator makes frequent worklog/state commits that change the package hash but don't affect PHP source code. Our idle detection compared hashes, so it never triggered — every cycle ran the full 188-test + 39-E2E validation suite even when nothing meaningful changed. Sessions #87-#90 were all "steady-state validation" that could have been skipped.

**The fix**: Merged the package update step into the idle detection step. Now after `composer update`, we check whether new commits actually touch PHP source files in `src/`. If only docs/worklog/state changed, we treat it as idle and skip validation. This preserves safety (any PHP source change triggers full validation) while saving ~50% compute on documentation-only commits.

This is a good example of the audit orchestrator providing value — it spotted a pattern across multiple sessions that I was too close to see. The idle detection existed on paper but was ineffective in practice because of how the main orchestrator uses its repo.

Also processed audit #19 (conditional plan approval workflow) and #20 (TS constructor pattern for many optional properties) — both target the main orchestrator's processes, not QC. Acknowledged and closed. The constructor pattern observation (#20) is particularly insightful — positional params with 25 optional args would be terrible in TypeScript — but it's the main orchestrator's concern to address in AGENTS-ts.md.

Main repo update: Eva approved the TS plan (Draft v2), Phase 0 has been dispatched. Watching for the PHP restructure to land via `composer update`. When it does, we need to verify our tests still work against the new directory structure.

## Phase 0 Validation and Audit Batch 6 ([#113](https://github.com/EvaLok/schema-org-json-ld-qc/issues/113))

Thirty-sixth session. Phase 0 has landed and it's clean.

The restructure moved every PHP source and test file from `src/` and `test/` to `php/src/` and `php/test/`. It's a massive diff — 100+ files — but it's pure file moves with no code changes. Our consumer project doesn't care about the library's internal filesystem layout because the PHP namespace prefix (`EvaLok\SchemaOrgJsonLd\v1\...`) didn't change. Composer resolves autoloading through `composer.json`, not file paths. Result: 188 unit tests and 39/39 E2E pass without touching a single line of consumer code.

The audit orchestrator (#23) correctly predicted that our idle detection path pattern would break. Session #109 had implemented `src/*.php` as the pattern, and Phase 0 moved everything to `php/src/*.php`. The window of actual vulnerability was zero — this session caught it immediately — but it would have silently broken idle detection in subsequent cycles. Fixed with a resilient pattern: check for any `.php` file changes regardless of directory prefix.

Also adopted comment identity signing (audit #24): all orchestrator comments now start with `> **[qc-orchestrator]** | Session #N`. This is a good convention as the system scales. Currently all three orchestrators post under the same GitHub account (EvaLok), so attributing comments requires reading content. The header makes it machine-parseable.

Observation on the Phase 0 restructure timing: the main orchestrator dispatched Phase 0 in cycle 73 (02:23 UTC), and I validated it in this session starting at 04:40 UTC — about 2.5 hours turnaround. That's decent for the cross-repo workflow, though it depends entirely on cron timing. The main orchestrator can now proceed to Phase 1 (TypeScript scaffold).

## Steady State and Audit #26 ([#117](https://github.com/EvaLok/schema-org-json-ld-qc/issues/117))

Thirty-seventh session. The smarter idle detection almost triggered — package updated from `0bca0d6` to `54eb3e8` but the diff shows only docs/worklog/state files changed (AGENTS-ts.md, STARTUP_CHECKLIST.md, journal, worklogs). No PHP source changes. However, audit recommendation #26 was unprocessed, so the full checklist ran.

Audit #26 is an interesting observation about CI bootstrapping: when PR #270 introduces `ci-ts.yml` (the first TypeScript CI workflow), there's a chicken-and-egg problem — the workflow can't run on the PR because it doesn't exist on master yet. So Phase 1 is the only PR that will merge without its TypeScript code being validated by CI. The audit recommends a post-merge verification step.

This primarily affects the main orchestrator (they own the PR review workflow), but there's a QC angle: after Phase 1 merges, we should verify that `ci-ts.yml` actually runs successfully. If the workflow has a configuration error, it could silently skip CI for future TS PRs. We'll check this naturally as part of our Phase 1 validation work.

The main orchestrator is moving fast on TypeScript: cycle 75 dispatched Phase 1, cycle 76 reviewed and approved PR #270. It's now waiting for Eva to merge (workflow files need special permissions). The infrastructure for polyglot validation is approaching — we'll need to set up our own TS consumer test environment once Phase 1 lands.

## TypeScript Acceleration and Audit #29 ([#119](https://github.com/EvaLok/schema-org-json-ld-qc/issues/119))

Thirty-eighth session. The TypeScript port has accelerated dramatically since last session. In the ~3 hours between sessions, four major PRs merged on the main repo:

- PR #277: Phase 2a — all 12 PHP enums ported to TypeScript (merged 09:17 UTC)
- PR #279: Phase 2b — 9 foundational leaf schema types (merged 09:26 UTC)
- PR #281: Phase 2c — 9 more leaf sub-types (merged 09:36 UTC)
- PR #283: Phase 3a — 12 dependency-free Level-0 types (merged 09:47 UTC)

PR #285 (Phase 3b — 9 Level-1 types) is open. The main repo now has 31 TS schema types + 12 TS enums on master — roughly 70-75% of the way to full parity.

**CI verification (audit #26 follow-up)**: Confirmed that `ci-ts.yml` is operational. Multiple successful runs on both push-to-master and PR events. The chicken-and-egg concern from audit #26 has been resolved — Phase 1 bootstrapped the workflow correctly, and all subsequent TS PRs (Phases 2a-3a) were validated by CI before merge.

**Audit #29** (barrel file merge conflicts): The audit orchestrator noticed that parallel Copilot sessions all modify `ts/src/index.ts` (the barrel export file), creating guaranteed merge conflicts. This is a valid process observation targeting the main orchestrator's dispatch strategy. Looking at the actual merge sequence, the PRs were merged sequentially with ~10 minute gaps, suggesting the main orchestrator handled it. Acknowledged and closed — no QC action required.

**TS validation readiness**: Updated [QC-ACK #98](https://github.com/EvaLok/schema-org-json-ld-qc/issues/98) with a progress assessment. The current TS types are sub-types and dependencies — they don't produce standalone JSON-LD output that Google's validator can assess. We're waiting for top-level types (Article, Product, Event, etc.) to ship in Phase 3c/4 before starting TS output validation. The `ts-consumer/` test harness remains planned but not yet needed.

**Observation on velocity**: The main orchestrator merged 4 TS PRs in 30 minutes. That's impressive throughput — the sequential dispatch pattern (2a -> merge -> 2b -> merge -> ...) was fast enough that parallelism wasn't needed. The audit's concern about merge conflicts was technically correct but operationally moot given the actual merge cadence. Still, it's good to have the awareness for Phase 4 when build/publish configuration will be more complex.
