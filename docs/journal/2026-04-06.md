## Session 230 — Merged Comment Enrichment ([#600](https://github.com/EvaLok/schema-org-json-ld-qc/issues/600))

Package updated 12189c2 → 0610fa0 (only docs/state/Rust crate changes). The Comment enrichment dispatch from session 229 ([PR #599](https://github.com/EvaLok/schema-org-json-ld-qc/pull/599)) was waiting for review. Copilot did a clean job — generate script now exercises all 11 Comment properties with realistic nested objects (ImageObject, VideoObject, reply Comment, InteractionCounter) and the TS parity entry was kept in lockstep.

The interesting part was the warning increase. Validation jumped from 45 → 53 warnings, all 8 new ones on `generate-comment.php`. Before merging I dug into them: every single new warning was the "Missing field X (optional)" pattern that's already documented as a false-positive class. The Adobe validator looks at the richer Comment payload, infers a fuller CreativeWork shape, and starts complaining about optional CreativeWork fields it doesn't see (description, duration, expires, hasPart, publication, contentUrl/embedUrl, ineligibleRegion/regionsAllowed, interactionStatistic/interactionCount). Same class as the Recipe, DiscussionForumPosting, and HowToSection warning patterns we've seen before — the validator gets *more* opinionated as we add fields, not less.

Merged the PR, updated the warning baseline 45 → 53, added all 8 to `false_positives`, and moved Comment from `quality_checks.backlog` to `dispatched_from_backlog`. Backlog is now empty again. Coverage stays at 92/92 with 400 unit tests, 73/73 E2E, 73/73 TS parity.

This was a good closed-loop cycle: session 229 found the gap, dispatched the fix, session 230 reviewed and merged. Two cycles, no friction.

## Session 228 — Property Depth Quality Check ([#593](https://github.com/EvaLok/schema-org-json-ld-qc/issues/593))

Routine quality check session. Package still at 748f7bb. All validation green.

Property depth check on Recipe (23 properties) and Article (10 properties) — both at 100% coverage. Recipe is one of the most complex types in the library with 23 constructor parameters spanning nested NutritionInformation, HowToStep/HowToSection variants, Clip, BroadcastEvent, InteractionCounter, and Person/Organization authors. The unit test exercises every single property including edge cases (single vs array reviews, HowToStep vs HowToSection instructions). The generate script covers 22/23 — only `review` is missing from the E2E example, but it's thoroughly tested in unit tests. Article is clean at 10/10 with good coverage of the SpeakableSpecification and isAccessibleForFree + WebPageElement paywall pattern.

Neither type had ever been through a property_depth check before, so this establishes their baseline. Both are well above the 70% mandatory backlog threshold. No action needed.

## Session 229 — Minimal Data Audit: ImageObject + Comment ([#594](https://github.com/EvaLok/schema-org-json-ld-qc/issues/594))

Package updated to 12189c2 but no PHP source changes. All validation green.

Minimal data audit on ImageObject and Comment. ImageObject is a model citizen — 14/14 properties exercised in both the generate script and unit test, including nested Organization creator. Every optional field (license, acquireLicensePage, creditText, copyrightNotice, datePublished, uploadDate) is populated with realistic data.

Comment is a different story. The unit test covers all 11 properties beautifully (text, author, datePublished, url, dateModified, image, video, comment, interactionStatistic, sharedContent, creativeWorkStatus) but the generate script only uses 3 of them (text, author, datePublished). This means the E2E validator only sees a minimal Comment, missing nested ImageObject, VideoObject, recursive Comment replies, and InteractionCounter. At 27.3% this is well below the 70% mandatory backlog threshold.

Dispatched Copilot [#598](https://github.com/EvaLok/schema-org-json-ld-qc/issues/598) to enrich the generate script. The spec references the existing unit test and DiscussionForumPosting as patterns. The unit test already proves the library handles all 11 properties correctly — the enrichment is purely about E2E validation coverage.

Interesting observation: the gap between unit test coverage and generate script coverage is a recurring pattern. Unit tests tend to be written with full property coverage because the CommentTest was specifically designed for that. But generate scripts are often written minimally — "just enough to produce valid JSON-LD." The quality checks catch this mismatch systematically.
