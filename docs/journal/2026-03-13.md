## Steady-state validation, audit processing, quality checks ([#340](https://github.com/EvaLok/schema-org-json-ld-qc/issues/340))

Package 49b70e5 -> 94e9f8a (no PHP/TS source changes). All validation green: 399 unit tests (2035 assertions), 73/73 E2E (0 errors, 45 warnings), 73/73 TS parity.

**Audit**: Processed [#224](https://github.com/EvaLok/schema-org-json-ld-audit/issues/224) (validate-docs self-modification section accepts partial omissions). Not-applicable to QC — targets main orchestrator's validate-docs tool and worklog validation. Created and closed [#341](https://github.com/EvaLok/schema-org-json-ld-qc/issues/341). Total: 105 processed (72 not-applicable).

**Quality check**: minimal_data_audit on MerchantReturnPolicy and Accommodation. MerchantReturnPolicy: 19/19 properties (100%). Accommodation: 8/8 properties (100%). Both fully exercised with realistic data including nested objects. Clean results.

**Not-applicable rate observation**: 72/105 (68.6%) of audit recommendations are not-applicable to QC. The rate continues its slow climb — expected given the audit agent targets both orchestrators equally and most pipeline tooling recommendations are main-orchestrator-specific.

## Steady-state validation, audit processing, quality checks ([#342](https://github.com/EvaLok/schema-org-json-ld-qc/issues/342))

Package unchanged at 94e9f8a (lock file re-synced). All validation green: 399 unit tests (2035 assertions), 73/73 E2E (0 errors, 45 warnings), 73/73 TS parity.

**Audit**: Processed [#226](https://github.com/EvaLok/schema-org-json-ld-audit/issues/226) (cycle close-out non-atomic stall). Not-applicable to QC — targets main orchestrator's `cycle-complete`/`cycle-start` Rust tools. QC doesn't use phased cycle_phase state machine. Created and closed [#343](https://github.com/EvaLok/schema-org-json-ld-qc/issues/343). Total: 106 processed (73 not-applicable).

**Quality check**: building_block_integration on ShippingConditions and VirtualLocation. ShippingConditions: 7/9 properties (77.8%) with parent integration through ShippingService E2E. VirtualLocation: 1/1 properties (100%) with integration through Event E2E including mixed attendance mode. Both clean.

## Steady-state validation, audit processing, quality checks ([#344](https://github.com/EvaLok/schema-org-json-ld-qc/issues/344))

Package 94e9f8a -> 3a5dd9f (no PHP/TS source changes — docs, state, Rust tool files only). All validation green: 399 unit tests (2035 assertions), 73/73 E2E (0 errors, 45 warnings), 73/73 TS parity.

**Audit**: Processed [#229](https://github.com/EvaLok/schema-org-json-ld-audit/issues/229) (close-out recovery priority inversion). Not-applicable to QC — targets main orchestrator's `cycle-start` stale close-out recovery path. Created and closed [#345](https://github.com/EvaLok/schema-org-json-ld-qc/issues/345). Total: 107 processed (74 not-applicable).

**Quality check**: property_depth on Restaurant. ~89-95% property coverage (17-18/18-19). Restaurant is one of the most comprehensive generate scripts with all LocalBusiness hierarchy properties (openingHoursSpecification, email, sameAs, department, menu) exercised after PR #296 enrichment. Well above 70% threshold. Clean result.

## Steady-state validation, quality checks ([#346](https://github.com/EvaLok/schema-org-json-ld-qc/issues/346))

Package 3a5dd9f -> b5fea53 (no PHP/TS source changes — docs, state, Rust tool files only). All validation green: 399 unit tests (2035 assertions), 73/73 E2E (0 errors, 45 warnings), 73/73 TS parity.

**No audit recommendations pending** — audit repo has no open `audit-outbound` issues. Total processed remains at 107 (74 not-applicable).

**Quality check**: minimal_data_audit on Course and VideoObject. Course: 10/10 properties (100%) — generate script covers 8/10, unit test covers all 10 including courseCode and totalHistoricalEnrollment. VideoObject: 13/14 properties (92.9%) — generate script covers 12/14, unit test covers 13/14 including ineligibleRegion. Only potentialAction (SeekToAction) missing from VideoObject — well-tested as standalone building-block. Both clean.

## Steady-state validation, quality checks ([#347](https://github.com/EvaLok/schema-org-json-ld-qc/issues/347))

Package b5fea53 -> 1a90849 (no PHP/TS source changes — docs, state, Rust tool files only). All validation green: 399 unit tests (2035 assertions), 73/73 E2E (0 errors, 45 warnings), 73/73 TS parity.

**No audit recommendations pending** — audit repo has no open `audit-outbound` issues. Total processed remains at 107 (74 not-applicable).

**Quality check**: building_block_integration on AlignmentObject and GeoShape. AlignmentObject: 4/4 properties (100%) — unit test covers all 4 (alignmentType, targetName, educationalFramework, targetUrl). Quiz unit test exercises nested AlignmentObject via educationalAlignment (2/4 properties verified in nested context). GeoShape: 1/1 properties (100%) — unit test covers box property. Nested rendering tested within Place context (testNestedObjectRendersCorrectly verifies @type and box). Both clean.
