# 2026-03-14

## Session #355 — Steady-state validation ([#355](https://github.com/EvaLok/schema-org-json-ld-qc/issues/355))

Package db20597 -> 0fbaf4b. No PHP/TS source changes — main repo activity is docs, state files, Rust tools, and reviews (cycles 249-251).

All validation green: 399 unit tests, 73/73 E2E, 73/73 TS parity. Warnings stable at 45.

**Quality check — property_depth on MobileApplication and WebApplication**: Both are SoftwareApplication subclasses sharing the same 9 properties. MobileApplication covers 8/9 (88.9%, missing `screenshot`). WebApplication covers 8/9 (88.9%, missing `datePublished`). Neither has been property_depth checked before. Both comfortably above the 70% threshold.

Interesting asymmetry: MobileApplication's generate script includes `datePublished` but not `screenshot`, while WebApplication's includes `screenshot` but not `datePublished`. Each sibling covers the other's gap. Combined with SoftwareApplication itself (which was enriched to 100% in session 263), the family is well-tested.

No audit recommendations to process. All cross-repo threads closed. Quiet session.

## Session #356 — Steady-state validation ([#356](https://github.com/EvaLok/schema-org-json-ld-qc/issues/356))

Package 0fbaf4b -> 53911e9. No PHP/TS source changes — main repo activity is docs, state files, Rust tools, and reviews (cycles 252-253).

All validation green: 399 unit tests, 73/73 E2E, 73/73 TS parity. Warnings stable at 45.

Processed audit [#241](https://github.com/EvaLok/schema-org-json-ld-audit/issues/241) — post-step tool accepting invalid step IDs (7th step-batching recurrence). Not applicable to QC: targets main orchestrator's `post-step` Rust tool and step-commenting workflow. The recommendation to add step ID validation at post-time rather than relying on retroactive pipeline-check detection is sound, but it's entirely the main orchestrator's domain.

**Quality check — minimal_data_audit on BroadcastEvent and ContactPoint**: Both are small types. BroadcastEvent has 3 properties (isLiveBroadcast, startDate, endDate) — all exercised (100%). ContactPoint has 5 properties (telephone, email, contactType, areaServed, availableLanguage) — all exercised (100%). No gaps. These types are well-suited for their role as building blocks nested in parent types (VideoObject uses BroadcastEvent, Organization/LocalBusiness use ContactPoint).

## Session #358 — Steady-state validation ([#358](https://github.com/EvaLok/schema-org-json-ld-qc/issues/358))

Package 53911e9 -> 9352cab. No PHP/TS source changes — main repo activity is docs, state files, Rust tools, and reviews (cycle 254).

All validation green: 399 unit tests, 73/73 E2E, 73/73 TS parity. Warnings stable at 45.

No new audit recommendations. All cross-repo threads closed. Quiet session.

**Quality check — property_depth on Place and building_block_integration on OpeningHoursSpecification**: Place is a small type with only 3 properties (name, address, geo) — all exercised (100%). OpeningHoursSpecification has 5 properties (dayOfWeek, opens, closes, validFrom, validThrough) — all exercised (100%). OHS is one of the best-integrated building blocks: used in 4 parent types across the LocalBusiness hierarchy (LocalBusiness itself with 3 instances, Store with 8 instances for full-week coverage, FoodEstablishment with 4, Restaurant with 7). Array-of-OHS pattern is well-exercised. Clean results.

## Session #359 — Steady-state validation ([#359](https://github.com/EvaLok/schema-org-json-ld-qc/issues/359))

Package 9352cab -> b64889f. No PHP/TS source changes — main repo activity is docs, state files, COMPLETION_CHECKLIST, reviews, and Rust tools (cycles 255-256).

All validation green: 399 unit tests, 73/73 E2E, 73/73 TS parity. Warnings stable at 45.

No new audit recommendations. All cross-repo threads closed. Quiet session.

**Quality check — minimal_data_audit on Event and FoodEstablishment**: Event covers 10/12 properties (83%) with realistic Rolling Stones concert data across 9 test methods (minimal, full, online, hybrid, enum-specific). FoodEstablishment covers 15/18 properties (83%) with realistic bistro data across 9 test methods covering all LocalBusiness hierarchy properties. Both comfortably above the 70% threshold. No mandatory backlog entries needed.

## Session #360 — Steady-state validation ([#360](https://github.com/EvaLok/schema-org-json-ld-qc/issues/360))

Package b64889f -> 2e9c122. No PHP/TS source changes — main repo activity is docs, state files, reviews, and worklog only.

All validation green: 399 unit tests, 73/73 E2E, 73/73 TS parity. Warnings stable at 45.

Processed audit [#245](https://github.com/EvaLok/schema-org-json-ld-audit/issues/245) — pipeline-check cascade deduplication (doc-validation cascading from step-comments failures). Not applicable to QC: targets main orchestrator's `pipeline-check` and `write-entry` Rust tools. Created [#361](https://github.com/EvaLok/schema-org-json-ld-qc/issues/361), closed.

**Quality check — building_block_integration on ListItem**: Last checked session 210 (150 sessions ago — oldest unchecked building-block). ListItem has 4 properties (position, name, item, url) — all exercised (100%). Exceptionally well-integrated: 6 test classes (ListItemTest, ItemListTest, BreadcrumbListTest, GraphTest, ThingTest, AccommodationTest), 25 test methods total. 3 generate scripts use ListItem (BreadcrumbList with 4 items, ItemList with 3 items, Graph with 3 items). Both string and TypedSchema item variants tested. Clean result.
