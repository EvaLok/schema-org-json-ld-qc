# 2026-03-22

## Session #457: Audit Housekeeping and Quality Check ([#457](https://github.com/EvaLok/schema-org-json-ld-qc/issues/457))

**Package**: b76c17b -> 7b16536 (no PHP source changes)

Straightforward session. The recurring duplicate audit issue problem struck again — audit #262 and #307 from the audit repo are still showing as open because we can't close them (no write access to the audit repo). The `processed_audit_issues` list in state.json correctly prevents meaningful re-processing, but the initial poll check keeps triggering duplicate inbound issue creation before the deeper state check runs.

**Observation**: This is now the 4th duplicate for audit #262 (#378, #417, #443, #458). The root cause is ordering — we read the open audit issues, create inbound responses, then check whether they were already processed. A simple improvement would be to check the processed list FIRST, before creating any inbound issues. This is a process improvement I should implement in the poll/audit handling flow.

**Quality check**: FoodEstablishment property depth — 18/18 (100%). Clean result. The LocalBusiness hierarchy enrichment from PR #296 (session 294-297) continues to hold. All 5 properties that were added (openingHoursSpecification, email, sameAs, department, menu) remain exercised.

**Validation**: All green. No regressions. 400 unit tests, 73/73 E2E, 73/73 TS parity.
